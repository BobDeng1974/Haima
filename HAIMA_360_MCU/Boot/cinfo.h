
// include once for function.c, both boot & app code

static ctrl_info_t cinfo =
{
	0, // host state, will change
	0, // host not ready at default
	0, // revers state, will change
	0, // upgrade type, will set before call
	0, // canwake_init

};

